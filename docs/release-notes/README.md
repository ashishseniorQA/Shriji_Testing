# docs/release-notes/

QA sign-off notes per release — distinct from product release notes. Name files
`<release-version-or-date>-qa-signoff.md`.

Each should state: suites run and results, known issues shipped with (with severity and linked
issue-tracker rows), environments verified, and explicit sign-off/no-sign-off with owner name.
