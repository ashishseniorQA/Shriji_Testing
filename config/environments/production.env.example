# Production — READ-ONLY smoke checks only. No write, no data creation, no destructive actions.
# Do not run full E2E, regression, or security-scan suites against this environment.
# Requires explicit user confirmation for the session before use — see root CLAUDE.md rules.
BASE_URL=https://app.fee-management.example.com
API_BASE_URL=https://api.fee-management.example.com
ENVIRONMENT=production
ALLOW_WRITE_OPERATIONS=false
ALLOW_DESTRUCTIVE_OPERATIONS=false
